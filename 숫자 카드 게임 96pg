# 행, 열

a, b = map(int, input().split()) # 행, 열 구분하기

c = [] # 각 줄별로 제일 작은 값들 보관할 리스트 생성
for i in range(a): # 행만큼 반복
    d = list(map(int, input().split())) # 입력값 리스트로 변환
    c.append(min(d)) # 입력된 리스트 중 최솟값 리스트c에 추가

print(max(c)) # 전체 최솟값 중 가장 큰 값 출력
